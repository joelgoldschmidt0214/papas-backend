{
  "name": "FastAPI Dev Container",
  "build": {
    // ビルドコンテキストを .devcontainer ディレクトリの一つ上の階層に設定
    "context": "..",
    // 開発用のDockerfileを指定
    "dockerfile": "../Dockerfile.dev"
  },
  // Dev Container Features: 追加機能をインストール
  "features": {
    // 一般的な開発ツール (Git, Zshなど) をインストール
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "installOhMyZsh": true,
      // ユーザー名をvscodeから変更しない (デフォルトのまま)
      "username": "vscode",
      "userUid": "automatic",
      "userGid": "automatic",
      // Zshをデフォルトシェルとして設定
      "configureZshAsDefaultShell": true
    }
  },
  // VS Codeのカスタマイズ設定
  "customizations": {
    "vscode": {
      // コンテナ内で有効にするVS Code拡張機能
      "extensions": [
        // --- 基本セット ---
        "ms-python.python",
        "charliermarsh.ruff",
        // --- Git関連 (チームで統一) ---
        "mhutchie.git-graph", // SourceTreeを表示
        "donjayamanne.githistory", // ファイルのGit履歴を表示
        // --- 開発効率化 ---
        "github.vscode-github-actions", // GitHub Actionsのワークフローを支援
        "gruntfuggly.todo-tree", // コード内のTODOコメントをツリー表示
        "christian-kohler.path-intellisense", // ファイルパスを補完
        "oderwat.indent-rainbow", // インデントを色付けして見やすくする
        "shardulm94.trailing-spaces", // 不要な末尾スペースを可視化・削除
        // --- Markdown編集用 ---
        "yzhang.markdown-all-in-one", // Markdown編集のオールインワンツール
        "davidanson.vscode-markdownlint", // Markdownの品質をチェック
        // --- データベース (オプション) ---
        "qwtel.sqlite-viewer" // SQLiteの内容表示
      ],
      // コンテナ内でのみ適用するVS Code設定
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        // Ruffをフォーマッターとして設定し、保存時に自動フォーマット
        "[python]": {
          "editor.defaultFormatter": "charliermarsh.ruff",
          "editor.formatOnSave": true
        }
      }
    }
  },
  // コンテナのポート8000をホストPCに転送
  "forwardPorts": [
    8000
  ],
  // コンテナ作成後に一度だけ実行されるコマンド
  // ここで依存関係をインストールする
  "postCreateCommand": "pip install -r requirements.txt",
  // VS Codeがコンテナに接続するたびに実行するコマンド
  // アプリケーションの正規の起動スクリプトである startup.py を実行する
  "postAttachCommand": "python startup.py",
  "remoteUser": "vscode",
  // ローカルフォルダをコンテナにマウントする設定
  "mounts": [
    {
      "source": "${localWorkspaceFolder}",
      "target": "${containerWorkspaceFolder}",
      "type": "bind"
    }
  ],
  // Gitのソース管理でdocsを表示させない設定
  "workspaceFolder": "/workspace"
}